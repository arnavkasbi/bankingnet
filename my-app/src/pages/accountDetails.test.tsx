import React from "react";
/*import { shallow, mount, render } from "enzyme";

import AccountDetails from "./accountDetails";

describe("<AccountDetails />", () => {
  it("renders without crashing", () => {
    shallow(<AccountDetails />);
  });

  it("snap shot of AccountDetails components", () => {
    const registerApp = shallow(<AccountDetails />);

    expect(registerApp).toMatchSnapshot();
  });
  it("find the back button", () => {
    let mntWrapper = mount(<AccountDetails />);
    const wrapper = shallow(<AccountDetails />);

    expect(mntWrapper.find("button").length).toBe(1);
  });
});*/
